# Protocol Types Provenance

The files in this directory are synced from the public VS Code repository only.

Canonical source path in VS Code:

- `src/vs/platform/agentHost/common/state/protocol`

Policy:

- Do not hand-edit generated protocol type files in this folder.
- When updating, sync from the VS Code source path above.
- Keep `version/registry.ts` synced from the same source tree.
